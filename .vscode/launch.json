{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Server",
            "program": "${workspaceFolder}/server/server.mjs",
            "runtimeVersion": "20",
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}